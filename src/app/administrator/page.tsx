import InfoPanel from "@/app/administrator/components/InfoPanel";

const Page = () => {

    return (
        <main className={'bg-[#FEFFFE] shadow-base py-2 px-3 mt-3'}>
            <InfoPanel/>
        </main>
    );
};

export default Page;